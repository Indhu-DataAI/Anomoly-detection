# Smart System Anomaly Detection Project Requirements

## Backend (Python) Requirements
# Navigate to backend/ directory and run: pip install -r requirements.txt

# Core ML/Data Science Libraries
pandas>=1.5.3
numpy>=1.24.3
scikit-learn>=1.2.2
xgboost>=1.7.0
imbalanced-learn>=0.10.1
joblib>=1.2.0

# API Framework
fastapi>=0.95.0
uvicorn[standard]>=0.21.0
python-multipart>=0.0.6

# Data Validation
pydantic>=1.10.7

# File I/O
python-csv>=0.0.13

# Additional utilities (optional)
matplotlib>=3.7.1
seaborn>=0.12.2

## Frontend (React) Requirements
# Navigate to frontend/ directory and run: npm install
# Dependencies are managed in package.json

## Installation Instructions:

### Backend Setup:
# 1. Navigate to backend directory: cd backend
# 2. Create virtual environment: python -m venv venv
# 3. Activate virtual environment:
#    - Windows: venv\Scripts\activate
#    - Linux/Mac: source venv/bin/activate
# 4. Install dependencies: pip install -r requirements.txt
# 5. Train the model (if needed): python model.py
# 6. Start the API server: python main.py or uvicorn main:app --reload

### Frontend Setup:
# 1. Navigate to frontend directory: cd frontend
# 2. Install dependencies: npm install
# 3. Start development server: npm start

### Running the Application:
# 1. Start backend server (default: http://localhost:8000)
# 2. Start frontend server (default: http://localhost:3000)
# 3. Access the application at http://localhost:3000